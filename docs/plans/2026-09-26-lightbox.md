# 计划：自研灯箱

规格：`docs/specs/2026-09-26-lightbox-design.md`。

## 任务

1. **决策**：ADR-0015；ADR-0003 的 cdnjs 行改为指向 0015。
2. **实现**：`ink.js` 用自研模块替换 fancybox 两段；`style.min.css` 换掉 `.fancybox-ori-link`。
3. **下线**：`post.ejs` 去掉三行 CDN；`csp.js` 去掉 cdnjs。
4. **契约测试**：`test/lib/lightbox-source.test.js` 锁住「源码不再出现 jquery / fancybox / cdnjs」。
5. **文档**：THEME / ARCHITECTURE / SECURITY / GRAMMAR / BACKLOG / adr 索引。
6. **验收**：`npm test`；`npx hexo clean && npm run build` 后扫产物；文章页点开核对规格 §3。

## 顺序

先实现与下线，再测，再改文档。验证前必须 `hexo clean`（热 `public/` 会假绿，见台账 C2）。
